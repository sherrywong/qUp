<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.berkeley.cs160.qUp"
    android:versionCode="1"
    android:versionName="1.0">

    <uses-sdk
        android:minSdkVersion="14"
        android:targetSdkVersion="19"
        android:maxSdkVersion="19" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".qUpApplication"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activities.QueueListActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".activities.QueueActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <activity
                android:name=".activities.BusinessActivityForm"
                android:label="@string/business">
        </activity>

        <activity
                android:name=".activities.BusinessActivityMain"
                android:label="@string/business">
        </activity>
        
        <activity
                android:name=".activities.ReservationForm"
                android:label="@string/personal">
        </activity>
        
        <activity
                android:name=".activities.ReservationSearch"
                android:label="@string/personal">
        </activity>
               
        <activity
            android:name=".activities.MyQActivity"
            android:label="@string/personal">


            <intent-filter>
                <action android:name="android.nfc.action.TAG_DISCOVERED" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>

            <!--<data android:mimeType="text/*" />-->


        </activity>
        <activity
            android:name=".NFCTask.TagInHandler"
            android:label="@string/app_name">

        </activity>
        
    </application>

</manifest>
